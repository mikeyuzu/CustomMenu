# カスタムメニュー拡張ガイド

このドキュメントでは、CustomMenu アドオンに新しいメニュー項目や機能を追加する方法を説明します。リファクタリングにより、データ駆動型の構成になっているため、最小限の修正で項目を増やすことができます。

## 拡張の 3 ステップ

基本的には以下の 3 つのファイルを修正することで新しい項目を追加できます。

### 1. `message.lua` : 表示テキストの定義
メニューのラベルや説明文を定義します。

```lua
-- message.lua 内
main_menu = {
    items = {
        -- ... 既存の項目
        new_feature = '新しい機能', -- 追加
    }
},
```

### 2. `menu_definitions.lua` : メニュー構造の定義
項目の ID、ラベル、および選択時のアクション（挙動）を定義します。

```lua
-- menu_definitions.lua 内
menu_definitions.main_menu = {
    items = {
        -- ... 既存の項目
        { 
            id = 'new_feature', 
            label = messages.main_menu.items.new_feature, 
            type = menu_definitions.types.FUNCTION, -- アクションタイプ
            func_name = 'Handle_New_Feature'        -- 実行する関数名
        },
    }
}
```

#### アクションタイプ (`type`) の種類
| タイプ | 説明 | 必要なパラメータ |
| :--- | :--- | :--- |
| `SUBMENU` | 定義済みの別のメニューを表示 | `submenu_id` (定義名) |
| `FETCH` | 外部 API からデータを取得して表示 | `id` (APIエンドポイント名) |
| `FUNCTION` | Lua の関数を直接実行 | `func_name` (関数名) |

### 3. `CustomMenu.lua` : ロジックの実装
`FUNCTION` タイプを指定した場合、実行される関数を実装し、`Handle_Confirm` に登録します。

#### 関数の作成
```lua
-- CustomMenu.lua 内 (Handle_Confirm の上あたり)
local function Handle_New_Feature()
    -- ここに処理を記述
    print('新しい機能が実行されました')
    
    -- 必要なら新しいメニューを表示
    local data = {
        title = '新メニュー',
        items = {
            { id = 'sub_1', label = 'サブ項目1' }
        }
    }
    param.set_current_menu(menu_manager.create_submenu(data))
    ui.show_menu_list(param.get_current_menu())
end
```

#### `Handle_Confirm` への登録
```lua
-- CustomMenu.lua 内の Handle_Confirm 関数
elseif selected.func_name == 'Handle_New_Feature' then
    Handle_New_Feature()
```

---

## 既存の便利な仕組みの活用

### API からデータを取得する場合 (`FETCH`)
`type = menu_definitions.types.FETCH` を指定すると、`http_handler.lua` を通じて自動的に API リクエストが飛び、返ってきた JSON データに基づいてサブメニューが生成されます。

### 合成倉庫のカテゴリを利用する場合
`synergy_category_generator.lua` を利用すると、アイテムの所持状況に基づいたカテゴリメニューを簡単に生成できます。

```lua
-- 使用例
local generated_menu = synergy_category_generator.generate_menu_data(inventory_data, 'main')
```

## 注意点
- 新しい `id` を追加した際、それが動的なパターン（例: `RECIPE_ITEM_` で始まる等）に合致しない場合は、自動的にデフォルトのアクションが実行されます。
- メニューの階層を深くする場合、`menu_manager.create_submenu` を使うことで、自動的に「戻る」ボタン（ESCキー）の履歴スタックが管理されます。
